# Primer contacto con GIT - GITHub	
> **Stack:** `Git` | `Github`

Repositorio de prueba para testear el funcionamiento de git

## 🛠️ Resumen de Competencias en Git

Para la gestión de este repositorio, se han aplicado técnicas profesionales de control de versiones que garantizan un historial limpio
y recuperable.

* **Estrategia de Ramas:** Uso de ramas independientes (`styled`, `htmlify`, `title`) para el desarrollo de funciones.
* **Resolución de Conflictos:** Gestión manual de conflictos de fusión (`merge conflicts`) asegurando la integridad del código.
* **Administración del Historial:** Dominio de herramientas de recuperación como `git reflog` y `git reset --hard` para volver a estados
específicos del proyecto.
* **Hitos de Versión:** Implementación de etiquetas (`git tag`) para marcar versiones estables: `inicial`, `styled`, `htmlify` y `title`.
* **Flujo Remoto:** Sincronización precisa con GitHub, gestión de ramas remotas y limpieza de referencias.
